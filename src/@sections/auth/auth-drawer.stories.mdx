import { Meta, Story, Canvas } from '@storybook/addon-docs';
import AuthDrawer from './auth-drawer';

<Meta
  title="Sections / Auth Drawer"
  component={AuthDrawer}
/>

# Auth Drawer

A component to display a `Auth Drawer`.

[View source](https://github.com/actarian/design-system/tree/main/src/@sections/auth-drawer)

## Imports

```js
import { AuthDrawer } from '@design-system';
```

## Example

A basic example of `Auth Drawer`.

<Canvas>
  <Story name="Auth Drawer">
    <AuthDrawer visible={true} onClose={() => alert} />
  </Story>
</Canvas>
