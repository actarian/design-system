import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Section, Container } from '@components';
import Auth from './auth';

<Meta
  title="Sections / Auth"
  component={Auth}
/>

# Auth

A component to display a `Auth`.

[View source](https://github.com/actarian/design-system/tree/main/src/@sections/auth)

## Imports

```js
import { Auth } from '@design-system';
```

## Example

A basic example of `Auth`.

<Canvas>
  <Story name="Auth">
    <Section>
      <Container>
        <Auth />
      </Container>
    </Section>
  </Story>
</Canvas>
