import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Divider, DividerProps } from './divider';
import { Media, Text } from '@components';

<Meta
  title="Components / Divider"
  component={Divider}
  decorators={[(story) => (
    <div style={ { padding: '2rem' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Divider

A component to display a `Divider`.

[View source](https://github.com/actarian/design-system/tree/main/@components/divider)

## Imports

```js
import { Divider } from '@design-system';
```

<!--
## Props

<ArgsTable of={DividerProps} />
-->

## Example

A basic example of `Divider`.

<Canvas>
  <Story name="Divider">
    <Divider></Divider>
  </Story>
</Canvas>

<Canvas>
  <Story name="with title">
    <Divider>Title</Divider>
  </Story>
</Canvas>
