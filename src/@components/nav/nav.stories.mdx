import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Nav, NavProps } from './nav';
import { Text, Button } from '@components';
import Link from 'next/link';

<Meta
  title="Components / Nav"
  component={Nav}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Nav

A component to display a `Nav`.

[View source](https://github.com/actarian/design-system/tree/main/src/@components/nav)

## Imports

```js
import { Nav } from '@design-system';
```

<!--
## Props

<ArgsTable of={NavProps} />
-->

## Example

A basic example of `Nav`.

<Canvas>
  <Story name="Nav.Row">
    <Nav.Row gap="3rem">
      <Link href="#marketing"><Button as="a" type="eta">Solutions</Button></Link>
      <Link href="#analytics"><Button as="a" type="eta">Company</Button></Link>
      <Link href="#commerce"><Button as="a" type="eta">Magazine</Button></Link>
      <Link href="#insights"><Button as="a" type="eta">More</Button></Link>
    </Nav.Row>
  </Story>
</Canvas>

<Canvas>
  <Story name="Nav.Col">
    <Nav.Col marginBottom="2rem" style={{ fontSize: '0.9rem' }}>
      <Text size="8">Company</Text>
      <Link href="#about"><Button as="a" type="eta">About</Button></Link>
      <Link href="#blog"><Button as="a" type="eta">Blog</Button></Link>
      <Link href="#jobs"><Button as="a" type="eta">Jobs</Button></Link>
      <Link href="#press"><Button as="a" type="eta">Press</Button></Link>
      <Link href="#partners"><Button as="a" type="eta">Partners</Button></Link>
    </Nav.Col>
  </Story>
</Canvas>
