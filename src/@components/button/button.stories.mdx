import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { ShoppingCart } from '@icons';
import { Flex } from '@components';
import { Label } from '@forms';
import { default as Button, ButtonProps } from './button';

<Meta
  title="Components / Button"
  component={Button}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  argTypes={{
    label: {
      name: 'label',
      description: 'overwritten description',
      type: { name: 'string', required: false },
      defaultValue: 'Button',
      table: {
        // disable: true, // to disable row
        type: {
          summary: 'something short',
          detail: 'something really really long'
        },
        defaultValue: { summary: 'Hello' },
      },
      // control: false, // to disable control
      control: {
        type: 'text'
      }
    },
    type: {
      name: 'type',
      description: 'overwritten description',
      type: { name: 'Variant', required: false },
      defaultValue: null,
      options: [null, 'default', 'alfa', 'beta', 'gamma', 'delta'],
      control: { type: 'select' },
      table: {
        type: {
          summary: 'something short',
          detail: 'something really really long'
        },
        defaultValue: { summary: 'alfa' },
      },
      // if: { arg: 'label', truthy: false },
    }
  }}
  parameters={{
    backgrounds: { disable: true, grid: { disable: true } },
  }}
/>

# Button

Used to trigger an operation.

[View source](https://github.com/actarian/design-system/tree/main/@components/button)

## Imports

```js
import { Button } from '@design-system';
```

<!--
## Props

<ArgsTable of={ButtonProps} />
-->

## Basic

A basic example of `Button`.

export const Basic = ({ data, ...args }) => (<>
  <Button {...args} onClick={() => alert('click')}>Action</Button>
</>);

<Canvas>
  <Story name="basic" parameters={{
    docs: {
      source: {
        code: '<Button onClick={() => {}}>Button</Button>',
        language: "tsx",
        type: "auto",
      }
    },
  }}>
    {Basic.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="default">
    <Label>Default</Label>
    <Flex.Col flexDirectionSm='row' justifyContent="space-between" alignItems="center" gap="0.5rem" padding="1rem 0">
      <Button type="default" size="xs" as="button">Button Xs</Button>
      <Button type="default" size="sm" as="button">Button Sm</Button>
      <Button type="default" size="md" as="button">Button Md</Button>
      <Button type="default" size="lg" as="button">Button Lg</Button>
      <Button type="default" size="xl" as="button">Button Xl</Button>
    </Flex.Col>
  </Story>
  <Story name="alfa">
    <Label>Alfa</Label>
    <Flex.Col flexDirectionSm='row' justifyContent="space-between" alignItems="center" gap="0.5rem" padding="1rem 0">
      <Button type="alfa" size="xs" as="button">Button Xs</Button>
      <Button type="alfa" size="sm" as="button">Button Sm</Button>
      <Button type="alfa" size="md" as="button">Button Md</Button>
      <Button type="alfa" size="lg" as="button">Button Lg</Button>
      <Button type="alfa" size="xl" as="button">Button Xl</Button>
    </Flex.Col>
  </Story>
  <Story name="beta">
    <Label>Beta</Label>
    <Flex.Col flexDirectionSm='row' justifyContent="space-between" alignItems="center" gap="0.5rem" padding="1rem 0">
      <Button type="beta" size="xs" as="button">Button Xs</Button>
      <Button type="beta" size="sm" as="button">Button Sm</Button>
      <Button type="beta" size="md" as="button">Button Md</Button>
      <Button type="beta" size="lg" as="button">Button Lg</Button>
      <Button type="beta" size="xl" as="button">Button Xl</Button>
    </Flex.Col>
  </Story>
  <Story name="gamma">
    <Label>Gamma</Label>
    <Flex.Col flexDirectionSm='row' justifyContent="space-between" alignItems="center" gap="0.5rem" padding="1rem 0">
      <Button type="gamma" size="xs" as="button">Button Xs</Button>
      <Button type="gamma" size="sm" as="button">Button Sm</Button>
      <Button type="gamma" size="md" as="button">Button Md</Button>
      <Button type="gamma" size="lg" as="button">Button Lg</Button>
      <Button type="gamma" size="xl" as="button">Button Xl</Button>
    </Flex.Col>
  </Story>
</Canvas>

<Canvas>
  <Story name="with icon">
    <Button type="alfa"><span>Buy</span> <ShoppingCart /></Button>
  </Story>
</Canvas>
