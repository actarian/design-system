import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as ButtonGroup, ButtonGroupProps } from './button-group';
import { ChevronDown } from '@icons';
import { Button } from '@components';
import Link from 'next/link';

<Meta
  title="Components / ButtonGroup"
  component={ButtonGroup}
  decorators={[(story) => (
    <div style={ { padding: '2rem' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# ButtonGroup

A component to display a `ButtonGroup`.

[View source](https://github.com/actarian/design-system/tree/main/@components/button-group)

## Imports

```js
import { ButtonGroup } from '@design-system';
```

<!--
## Props

<ArgsTable of={ButtonGroupProps} />
-->

## Example

A basic example of `ButtonGroup`.

<Canvas>
  <Story name="default">
    <ButtonGroup>
      <Button type="default">Read documentation</Button>
      <Button type="default">View on Github</Button>
      <Button type="default"><span>Open</span> <ChevronDown /></Button>
    </ButtonGroup>
  </Story>
</Canvas>

<Canvas>
  <Story name="vertical">
    <ButtonGroup vertical>
      <Button type="default">Read documentation</Button>
      <Button type="default">View on Github</Button>
      <Button type="default"><span>Open</span> <ChevronDown /></Button>
    </ButtonGroup>
  </Story>
</Canvas>
