import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Layout, LayoutProps } from './layout';

<Meta
  title="Components / Layout"
  component={Layout}
  decorators={[(story) => (
    <div style={ { padding: '2rem' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Layout

A component to display a `Layout`.

[View source](https://github.com/actarian/wsdesign-system/tree/main/@components/card)

## Imports

```js
import { Layout } from '@wsdesign-system';
```

<!--
## Props

<ArgsTable of={LayoutProps} />
-->

## Example

A basic example of `Layout`.

<Canvas>
  <Story name="Layout">
    <Layout>Layout</Layout>
  </Story>
</Canvas>
