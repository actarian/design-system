import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Container, ContainerProps } from './container';

<Meta
  title="Components / Container"
  component={Container}
  decorators={[(story) => (
    <div style={ { padding: '2rem' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Container

A component to display a `Container`.

[View source](https://github.com/actarian/wsdesign-system/tree/main/@components/card)

## Imports

```js
import { Container } from '@wsdesign-system';
```

<!--
## Props

<ArgsTable of={ContainerProps} />
-->

## Example

A basic example of `Container`.

<Canvas>
  <Story name="Container">
    <Container padding="3rem 0">
      Container
    </Container>
  </Story>
</Canvas>
